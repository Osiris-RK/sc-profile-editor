# Overview

This command is a substitute for the claude `/exit` command that does several wrap-up tasks before invoking the `/exit` command.


# Invocation

This command is invoked with `/bye`

# Instructions

When invoked, perform the following steps:

1. Invoke the `/log-context` command.
2. Invoke the `exit` command. 

