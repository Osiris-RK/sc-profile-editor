# Star Citizen Profile Editor - Dependencies

# Core Dependencies (Required for all builds)
PyQt6>=6.10.0               # GUI framework with native QtPdf/QtPdfWidgets for PDF viewing
python-docx>=1.1.0          # Word document generation
reportlab>=4.0.0            # PDF generation
PyMuPDF>=1.26.0             # PDF manipulation and form field handling
Pillow>=10.1.0              # Image manipulation and graphics
python-dinput>=1.0.0        # Windows joystick/gamepad input detection
pynput>=1.7.0               # Keyboard and mouse input detection

# Optional Dependencies - DEPRECATED (No longer needed)
# OCR dependencies were used for SVG template development (now deprecated)
# The following dependencies are no longer required:
# easyocr>=1.7.0            # OCR for template tag detection (DEPRECATED)
# torch>=2.0.0              # PyTorch (DEPRECATED)
# opencv-python>=4.8.0      # OpenCV for image processing (DEPRECATED)

# Build Tools
pyinstaller>=6.3.0          # For building standalone executable
python-dotenv>=1.0.0        # Environment variable management for scripts

# Note: The project now uses PDF templates exclusively
# SVG/PNG templates and OCR utilities have been deprecated as of v0.4.0
